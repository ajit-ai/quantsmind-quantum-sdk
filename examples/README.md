# Examples

End-to-end usage examples. R0.1.0 is architecture-only, so examples
here currently demonstrate *intended* usage shape against interfaces,
not working numerical output. Populate per-domain examples as concrete
implementations land, e.g. `examples/quantum/bell_state.py`.
