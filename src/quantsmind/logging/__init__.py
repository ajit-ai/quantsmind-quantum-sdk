"""
Logging Package — Defines a vendor-independent logging interface used by every other package
instead of importing a logging library directly.

This package is part of the QuantsMind SDK (R0.1.0).
Architecture-only: no implementations, only public interface surface.
"""

__all__: list[str] = []
