# Utilities Package

## Purpose
Provides small, dependency-free helper interfaces shared across the SDK (e.g. identifiers, decorators, type helpers).

## Responsibility
Own generic, side-effect-free helper contracts only.

## Dependencies
- *(none — leaf package)*

## Future Interfaces
- Typed ID generation contracts
- Common decorator/protocol helpers

## Status
Architecture-only (R0.1.0). No scientific, numerical, or algorithmic
implementations exist in this package yet. All classes are interfaces,
abstract base classes, or empty skeletons documenting intended shape.

## Testing
See `tests/unit/utils`, `tests/integration/utils`
(placeholders only, no test logic yet).
