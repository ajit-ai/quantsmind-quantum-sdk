# Benchmarks

Long-running performance benchmarks (as opposed to `tests/performance`,
which enforces budgets in CI). Benchmarks here are exploratory and
tracked over time to catch performance regressions across releases.

Structure mirrors `src/quantsmind` packages. No benchmarks implemented
yet in R0.1.0 (architecture-only).
